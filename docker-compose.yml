version: '3'
services:
  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
  judger:
    image: judger
    restart: always
    command: python3 script.py
    depends_on:
      - mongo
    volumes:
      - ./judger:/app
      